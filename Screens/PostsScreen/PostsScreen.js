import { View, Text } from "react-native";
import { styles } from "./PostsScreenStyle";

export const PostsScreen = () => {
  return (
    <View>
      <Text style={styles.text}>PostsScreen page</Text>
    </View>
  );
};
